---
// Imports
import Layout from '../layouts/Layout.astro';
import '../styles/globals.css';

import Hero from "@/components/Hero.astro";
import LinkInLine from "@/components/LinkInline.astro"; 
import SectionContainer from "@/components/SectionContainer.astro";
import Link from '@/components/icons/Link.astro'; 
import Experience from '@/components/Experience.astro'; 
import SkillTree from '@/components/Tree.jsx'; // React component
import Mentoring from '@/components/Mentoring.jsx'; // React component
import Certifications from '@/components/Certifications.jsx'; // React component
import Projects from '@/components/Projects';
import Roles from '@/components/Roles';
import Services from '@/components/Services.astro';


---

<Layout title="MT - Soluciones Tecnológicas Innovadoras" description='El portafolio'>
    <div class="max-w-screen-xl mx-auto">
        <main class="bg-gradient-to-br from-gray-900/10 via-gray-800 to-purple-900/40 container px-6">
         <SectionContainer>
                <Hero />
            </SectionContainer>
                <SkillTree client:load></SkillTree>
            <SectionContainer>
                <Roles client:load/>
            </SectionContainer>

            <Services />
            <SectionContainer>
            
          </SectionContainer>
            

         

            <SectionContainer>
                <Projects client:load />
            </SectionContainer>
            <Mentoring /> 

            <Certifications client:load />
        </main>
    </div>
</Layout>

<style is:global>
    :root {
        color-scheme: light dark;
    }

    html {
        font-family: "Onest Variable", system-ui, sans-serif;
        scroll-behavior: smooth;
    }

    body {
        color: rgba(241, 237, 237, 0.9);
        display: flex;
        flex-direction: column;
        min-height: 100vh;
        overscroll-behavior: none;
    }

    @media (prefers-reduced-motion: reduce) {
        html {
            scroll-behavior: auto;
        }
    }

    @media (prefers-color-scheme: dark) {
        body {
            color: rgba(255, 255, 255, 0.9);
        }
    }

    #header-nav {
        animation: blur linear both 0.5s;
        animation-timeline: scroll();
        animation-range: 0 500px;
    }

    @keyframes blur {
        to {
            backdrop-filter: blur(20px);
            border-width: 1px;
            border-color: rgba(0, 0, 0);
            padding: 0.25rem 0.75rem;
            display: flex;
            flex-direction: row;
            align-items: center;
            background-color: rgb(229, 229, 229);
            border-radius: 9999px;
        }
    }

    @media (prefers-color-scheme: dark) {
        @keyframes blur {
            from {
                border: 0px;
            }
            to {
                box-shadow:
                    0px 5px 50px -5px rgba(0, 0, 0, 0.1),
                    0px 0px 0 1px rgba(0, 0, 0, 0.3);
                background: rgba(0, 0, 0, 0.3);
                backdrop-filter: blur(20px);
                border-width: 1px;
                border-color: rgba(0, 0, 0);
                padding: 0.25rem 0.75rem;
                display: flex;
                flex-direction: row;
                align-items: center;
                background-color: rgba(0, 0, 0, 0.3);
                border-radius: 9999px;
            }
        }
    }
</style>
